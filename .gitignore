# Local secrets — never commit. Generated at deploy time.
# config/ holds credentials and keys only; nothing in it belongs in git.
/config/database.php
/config/app.php
/config/*.local.php

# Machine-local Claude Code permissions
**/.claude/settings.local.json

# PHP / editor noise
*.log
.DS_Store

# Composer dependencies
/vendor/
